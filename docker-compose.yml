version: "3.9"

services:
  backend:
    build:
      context: ./backend
    container_name: reviewgen-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    container_name: reviewgen-frontend
    ports:
      - "3008:3000"
    depends_on:
      - backend
    restart: unless-stopped